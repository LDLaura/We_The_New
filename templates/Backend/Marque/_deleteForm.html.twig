<form action={{ path('admin.marque.delete', {id: marque.id}) }} method="POST", onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette marque ?')">
    <input type="hidden" name="token" value="{{ csrf_token('delete' ~ marque.id) }}">
    <button type="submit" class="btn btn-danger">Supprimer</button>    
</form>